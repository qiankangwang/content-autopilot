# assets/bgm/

Background-music tracks for `make_rich_video.py` (mixed low under the voice with
sidechain ducking). Not committed — supply your own **public-domain / royalty-free**
mp3s, e.g. from [FreePD](https://freepd.com/).

Name them with a mood prefix so `--bgm auto` can pick by content energy:

- `chill-*.mp3` — calm/explainer content (科普, 讲解)
- `upbeat-*.mp3` — high-energy content (爆点, 游戏, 快节奏)

Any mp3 dropped here joins the rotation automatically. `--bgm off` disables music.
